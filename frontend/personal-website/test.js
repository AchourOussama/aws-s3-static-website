



// Check if the 'count' element exists
const countElement = document.getElementById('count');

if (countElement) {
  // 'count' element exists
  console.log('Element with ID "count" exists.');
} else {
  // 'count' element does not exist
  console.error('Element with ID "count" does not exist.');
}
